[project]
name = "llm-inference"
version = "0.1.0"
description = "Add your description here"



requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.141.1",
    "httpx>=0.28.1",
    "uvicorn[standard]>=0.30",
]

[project.scripts]
llm-inference = "llm_inference:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
